{
  "name": "@architecture-flow/db",
  "private": true,
  "version": "0.1.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "migrate": "node ./scripts/migrate.mjs",
    "seed": "node ./scripts/seed.mjs",
    "build": "tsc -p tsconfig.json",
    "lint": "echo 'lint not wired yet'",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
